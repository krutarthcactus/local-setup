# =============================================================================
# SCR Platform — Local Docker Environment Configuration
# =============================================================================
# Copy this file to `.env` and adjust paths/credentials as needed.
# `.env` is committed with safe local-only defaults, but you can override
# anything here per-developer without touching docker-compose.yml.

# -----------------------------------------------------------------------------
# Source code paths (bind-mounted into containers)
# -----------------------------------------------------------------------------
# These point to the actual Laravel and React repos, which live OUTSIDE this
# SCR repo (siblings under ~/development/). Adjust if your layout differs.
# Relative paths are resolved relative to this local-docker/ directory.
BACKEND_PATH=../../scr-backend
FRONTEND_PATH=../../scr-frontend
WEBSOCKET_PATH=../../scr-websocket
# -----------------------------------------------------------------------------
# Host routing
# -----------------------------------------------------------------------------
# Add this to /etc/hosts: 127.0.0.1 local.scr.com
APP_DOMAIN=local.scr.com
#NGINX_PORT=8000

# -----------------------------------------------------------------------------
# PostgreSQL
# -----------------------------------------------------------------------------
POSTGRES_DB=scr_platform
POSTGRES_USER=scr
POSTGRES_PASSWORD=scr_local_password
POSTGRES_PORT=25432

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
REDIS_PORT=26379

# -----------------------------------------------------------------------------
# Vite (frontend dev server)
# -----------------------------------------------------------------------------
VITE_PORT=5173

# -----------------------------------------------------------------------------
# Node.js WebSocket Server (scr-websocket)
# -----------------------------------------------------------------------------
WEBSOCKET_PORT=3000
NODE_ENV=development

# -----------------------------------------------------------------------------
# Mailpit (catches all local outbound email)
# -----------------------------------------------------------------------------
MAILPIT_SMTP_PORT=11025
MAILPIT_UI_PORT=18025

# -----------------------------------------------------------------------------
# AWS S3 / Floci (local S3-compatible storage)
# -----------------------------------------------------------------------------
# Floci emulates AWS S3 locally without needing a real AWS account.
# The backend and queue services use these vars to connect to Floci.
FLOCI_PORT=4566
FLOCI_STORAGE_MODE=hybrid

# AWS credentials (for local Floci, these are test values)
# These are available as container env vars for shell/CLI convenience
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=scr-private-bucket
AWS_BUCKET_PRIVATE=scr-private-bucket
AWS_BUCKET_PUBLIC=scr-public-bucket

# S3 endpoint (points to Floci for local dev)
# Remove AWS_ENDPOINT in production to use real AWS S3
AWS_ENDPOINT=http://floci:4566
AWS_USE_PATH_STYLE_ENDPOINT=true

# -----------------------------------------------------------------------------
# Bootstrap behavior
# -----------------------------------------------------------------------------
# Set to "false" if you already have Laravel/React apps in BACKEND_PATH /
# FRONTEND_PATH and don't want the entrypoint scripts to touch them.
AUTO_BOOTSTRAP=true
